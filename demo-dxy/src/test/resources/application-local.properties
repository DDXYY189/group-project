# 测试使用内存 H2，避免与正在运行的应用争用 data/conversation-memory 文件锁
spring.datasource.url=jdbc:h2:mem:group-demo-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
mcp.enabled=false
