spring:
  profiles:
    active: local

server:
  port: 8080

llm:
  api-key: ${DASHSCOPE_API_KEY:}
  text-model: qwen-turbo
  vision-model: qwen-vl-plus
  asr-model: qwen-audio-turbo
  base-url: https://dashscope.aliyuncs.com/api/v1

wechat:
  enabled: true
  poll-interval: 1000
  auto-start: true
